kb-sync-test: multi-tier push verification 2026-07-24, safe to remove.
This file lives under artifacts/astrobot/public/ and must NOT be ingested
into the knowledge base — it exists only to confirm the exclude rule works.

kb-sync-test #2: this MODIFICATION should not appear in changedFiles for
the KB sync either — checks the "modified" array, not just "added".
