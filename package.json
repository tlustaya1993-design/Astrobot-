{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "normalize-avatars": "python scripts/normalize_avatar_circles.py --apply",
    "normalize-avatars-galactic": "python scripts/normalize_avatar_circles.py --apply --only artifacts/astrobot/public/avatar-presets/miss-galactica/*.webp",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test:e2e:smoke": "node scripts/e2e/smoke-chat-billing.mjs",
    "test:readiness": "node scripts/diagnostics/test-group-readiness.mjs",
    "test:financial-proofs": "tsx scripts/diagnostics/financial-safety-proofs.ts",
    "verify:postgres-schema": "node scripts/diagnostics/verify-postgres-schema.mjs",
    "diagnostics:api": "node scripts/diagnostics/api-health.mjs"
  },
  "private": true,
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.1",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.20.0",
    "pg-mem": "^3.0.14",
    "playwright": "^1.59.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.3.0"
  }
}
